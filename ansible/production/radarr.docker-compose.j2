
---
version: "2.1"
services:
  radarr:
    image: lscr.io/linuxserver/radarr:latest
    container_name: radarr
    environment:
      - PUID=0
      - PGID=0
      - TZ={{ timezone }}
    volumes:
      - {{ mnt_path }}/config:/config
      - {{ library_path }}:/library
      - {{ download_dest_path }}:/downloads/complete
    ports:
      - 7878:7878
    restart: unless-stopped
