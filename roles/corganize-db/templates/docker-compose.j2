services:
  corganize-db:
    build:
      context: ./crg-db
    ports:
      - {{ app_object.port }}:5822
    environment:
      TZ: America/Denver
    volumes:
      - {{ db_path }}:/database
      - {{ log_path }}:/var/log/corganize
