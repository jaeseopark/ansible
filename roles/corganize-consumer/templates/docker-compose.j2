services:
  consumer:
    build:
      context: .
      dockerfile: ./crg-consumer/Dockerfile
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - {{ log_path }}:/var/log/corganized-consumer
      - {{ data_path }}:/data
