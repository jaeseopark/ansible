# TODO resolve hosts (should be 1?)
- name: Transmission
  vars:
    app_path: /app/transmission
    media_path: /mnt/media
    trns_mnt_path: /app/transmission/trns-mnt
    trns_bkup_mnt_path: /app/transmission/trns-bkup-mnt
    trns_bkup_dl_path: /srv/dev-disk-by-uuid-5a0f2275-e689-459c-b6fb-cd14264bb915/crg/downloads
    docker_compose_path: /app/transmission/docker-compose.yml
    wg_conf_path: /app/transmission/wg0.conf
    wg_resolv_conf_path: /app/transmission/wg.resolv.conf
    wg_fwd_enabled: "false" # Mullvad stopped supporting it. keeping it disabled for now.
    tz: America/Denver
    puid: "0"
    pgid: "27"
- name: Arr suite
  roles:
    - arr
- name: Jellyfin
  roles:
    - jellyfin
